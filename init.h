#ifndef init_h
#define init_h

#include <iostream>
#include <map>
using namespace std;

vector<string> splitCommand(string command);
void initialize(std::map<std::string, std::string> &args);

#endif
